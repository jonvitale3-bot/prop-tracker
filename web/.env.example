# Copy to web/.env.local. Vercel will inject these in production.

# Use the *pooled* Neon connection string here (the one with -pooler in the host).
DATABASE_URL=postgres://USER:PASSWORD@HOST-pooler/DBNAME?sslmode=require
